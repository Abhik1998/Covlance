{
  "name": "server",
  "version": "1.0.0",
  "description": "server side of covlance",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start",
    "client:build": "cd client && npm install && npm run build",
    "start": "npm install && npm run client && node index.js"
  },
  "keywords": [
    "covid"
  ],
  "author": "abhik chakraborty",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "firebase": "^8.4.2",
    "firebase-admin": "^9.6.0",
    "mysql": "^2.18.1",
    "react-scripts": "^4.0.3"
  }
}
